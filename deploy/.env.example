# Jugar Deployment Environment Configuration
# ============================================
# Copy this file to .env and fill in the values.
# DO NOT commit .env to version control!
#
# Usage:
#   cp deploy/.env.example deploy/.env
#   # Edit deploy/.env with your values
#   source deploy/.env && make deploy-game GAME=pong

# Required: Object storage bucket name
# Example: my-games-bucket-abc123
JUGAR_DEPLOY_BUCKET=

# Required: CDN distribution identifier
# Example: E1234567890ABC
JUGAR_CDN_DISTRIBUTION=

# Required: Public URL where games will be accessible
# This is used for generating links and validation
JUGAR_DEPLOY_URL=https://interactive.paiml.com

# Optional: AWS profile to use (if not using default)
# AWS_PROFILE=my-profile

# Optional: AWS region (defaults to us-east-1)
# AWS_REGION=us-east-1

# Optional: Enable verbose deployment logging
# JUGAR_DEPLOY_VERBOSE=1
